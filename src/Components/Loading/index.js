import React from "react";
import styles from "./Loading.module.css";

const Loading = () => {
  return <div className={styles.LoadingMessage}>Veriler yükleniyor...</div>;
};

export default Loading;
